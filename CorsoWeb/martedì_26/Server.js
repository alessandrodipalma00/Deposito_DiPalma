// Importa il modulo
const saluta = require("./saluti");

// const http = require(http)
// http permette di esportare le funzioni. c'è un modulo dove puoi usare le sue funzioni
// se è un modulo personale basta lo slash, in una altra cartella viene richiamata la cartella/ la funzione senza il js finale

// Usa la funzione importata
console.log(saluta("Mirko"));